import { Text } from 'react-native';

import { ScreenContainer } from '../components/ScreenContainer';

export function EasyModeScreen() {
  return (
    <ScreenContainer
      title="TottiBeat"
      subtitle="Native Easy Mode foundation for the future mobile metronome experience."
    >
      <Text>Easy mode controls will land in a later batch.</Text>
    </ScreenContainer>
  );
}
